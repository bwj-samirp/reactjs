const URL = 'http://localhost:7777/';

const constants = {
    api: {
        app: URL + 'app',
        user: URL + 'user'
    },
    client_id: '',
    client_token: ''
};

const client_id = '';
const client_token = '';



export { URL as base, client_id, client_token };
export default constants;
